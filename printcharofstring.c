#include<stdio.h>
#include<string.h>
int main() 
{
      char s[100];
       int i,k;
      printf("input\n");
       scanf("%s",s),;
       printf("input limit\n");
       scanf("%d",&k);
       for(i=0;i<k;i++)
        {
            printf("%c",s[i]);
          }
	return 0;
}
